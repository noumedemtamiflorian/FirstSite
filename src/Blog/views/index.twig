{% extends 'layout.twig' %}
{% block title "Mes Article " %}
{% block body %}
    <h1>Bienvenue sur le blog</h1>
    <ul>
        {% for i in range(0,3) %}
            <li><a href="{{ path('blog.show', {slug:  'zaeaze'  }) }}-{{ i }}">Article {{ i+1 }} <?= $i + 1 ?> </a></li>
        {% endfor %} </ul>
{% endblock %}