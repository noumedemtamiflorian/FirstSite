<div class="col-sm-4">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">{{ count }}</h4>
            <p class="card-text">Article{% if count > 1 %}s{% endif %}</p>
            <a href="{{ path('admin.post.index') }}" class="btn btn-primary">Gerer Les Articles</a>
        </div>
    </div>
</div>